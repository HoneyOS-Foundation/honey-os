[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
anyhow = "1.0.82"
hapi = { path = "../hapi", features = ["logger"] }
log = "0.4.21"
wasm-bindgen = "0.2.92"
wasm_thread = "0.3.0"
